spring.application.name=tractor-bhai

# Supabase (PostgreSQL) Configuration
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?prepareThreshold=0
spring.datasource.username=postgres.jmiaujzzddiogewoisfe
spring.datasource.password=Kumar9060@.com
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Port
server.port=8080
server.address=0.0.0.0
spring.output.ansi.enabled=always

# Google OAuth2 Configuration
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email
 
# JWT Configuration
jwt.secret=${JWT_SECRET:tractor_bhai_super_secret_key_for_jwt_auth_2024_tractor_bhai_super_secret_key}
 
# Deployment Configuration
frontend.url=${FRONTEND_URL:http://localhost:5173}
